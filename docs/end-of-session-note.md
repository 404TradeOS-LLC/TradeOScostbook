# End Of Session Note

## Current State
- PostgreSQL 16 test infrastructure is available through `npm run test:integration`.
- The harness recreates `tradeos-costbook-test` on `127.0.0.1:55432`, then runs `scripts/deploy-migrations.sh` — the same automation a real production deploy uses (`prisma migrate deploy` against `prisma/migrations/`, then idempotent app-role provisioning via `scripts/provision-app-role.sh`). There is no longer a separate hand-rolled test-only path.
- `prisma/migrations/` (not `db/migrations/`, which no longer exists) is now the single authoritative, tracked migration history. `prisma migrate deploy` is the one production rollout command.
- Forced RLS is live-tested through that non-superuser role rather than the RLS-bypassing `postgres` fixture connection.
- Authenticated HTTP requests, internal admin HTML requests, and background jobs all establish transaction-local database context.
- Platform provisioning is isolated at `POST /api/v1/platform/organizations` behind `PLATFORM_PROVISIONING_SECRET` and creates the first owner atomically. It is now also rate-limited per client IP (`express-rate-limit`, default 5/15min) and supports an optional `PLATFORM_PROVISIONING_ALLOWED_IPS` allowlist, both checked before the secret comparison.
- Material price changes now write immutable old/new price audit records with source and actor metadata.
- Admins can read filtered price history at `GET /api/v1/admin/organizations/:id/pricing-history`; viewers cannot see those rows under RLS.
- The broader internal admin shell is mounted at `/admin` and `/admin/pricing-history`; membership history remains at `/admin/member-history`.
- Supplier-fed price changes are now staged through a queued-review table (`supplier_price_updates`) rather than applied directly: `POST/GET /api/v1/supplier-integrations/queue` and `POST .../queue/:id/approve|reject`. Approval applies the price and writes the same `material_price_audits` trail a manual edit would; only admin/owner can approve/reject under RLS, though estimator+ can enqueue. `modules/supplier-integration/worker.ts`'s `runSupplierPriceSyncJob` actually exercises `runWithBackgroundDatabaseSession` — it has no HTTP route and is meant for a future scheduler to call. The feed fetch itself is still a stub (no live supplier API exists yet); only the persistence/review/worker plumbing around it is real.
- Suppliers now have a full CRUD API at `/api/v1/suppliers` (`GET/POST /`, `GET/PATCH/DELETE /:id`). `apiIntegrationKey` is write-only — every read returns `hasApiIntegrationKey: boolean`, never the stored value. Deleting a supplier (or material) with any `supplier_price_updates` history is blocked at the database level (`ON DELETE RESTRICT`, the same protection `material_price_audits` gives materials) — this also fixed an `on delete cascade` from the prior session that would have silently destroyed that history on deletion instead.
- `runSupplierPriceSyncJob` now has two ways to actually run on a schedule instead of just existing as a callable function: an optional in-process `node-cron` timer started from `api/server.ts` (`SUPPLIER_PRICE_SYNC_CRON_SCHEDULE` + `SUPPLIER_PRICE_SYNC_JOBS`), or `npm run jobs:supplier-price-sync` for external cron/k8s CronJob/systemd timer use. Sync targets are listed explicitly (`{orgId, userId, supplierId, label?}`) rather than auto-discovered across organizations — deliberate, since discovery would need a database connection that bypasses RLS, which background jobs in this app are never given.
- The centralized error handler now maps Prisma constraint-violation codes to clean 4xx responses: P2002 (unique) → 409, P2003 (foreign key) → 409, P2025 (not found) → 404. Every controller benefits automatically. Anything else still falls through to the generic 500, including RLS-denial errors (not one of the mapped codes, so unaffected).
- Production migration rollout is now automated. `prisma/migrations/` baselines the old hand-written `db/migrations/0001`/`0002` SQL files verbatim (verified byte-for-byte schema equivalence and full live-RLS behavioral equivalence before deleting the originals). `scripts/deploy-migrations.sh` (`prisma migrate deploy` + idempotent app-role provisioning) is the one command for CI/CD or a manual release step; `scripts/provision-app-role.sh` can be run alone to rotate the role's password. Both require an elevated `DATABASE_ADMIN_URL` — the runtime `DATABASE_URL` role can never run DDL or grant privileges to itself.
- That script is now wired into `.github/workflows/deploy-migrations.yml`: manual `workflow_dispatch`, or auto-triggered on `main` pushes touching `app/prisma/migrations/**`, against a `production` GitHub Environment so required-reviewer approval can gate it. The repo has no git remote yet, so this has only been verified locally via `act` (genuinely runs the job in a real Actions runner image), never against actual GitHub infrastructure.
- `/admin/member-history` now shares the same visual shell as `/admin`/`/admin/pricing-history` (`api/views/adminShell.view.ts`'s layout and CSS) instead of being a separately-styled dark-theme page. All existing functionality (filters, chips, pagination, before/after snapshots) is unchanged — only the chrome changed.
- Assemblies can now be flagged `isTemplate: true` to mark them as common, reusable starting points (vs. one-off assemblies built for a specific job). `GET /api/v1/assemblies/templates` lists an org's templates. Adding one to an estimate uses the same `POST /api/v1/estimates/:id/line-items` with `assemblyId` that already existed and already resolved the rolled-up unit cost in one call — that "quick add" mechanism wasn't new, just the concept of a template to add. One example ("Residential Driveway Base Package") is seeded.

## Verification Evidence
- `npm test`: 56 passing tests across 16 suites.
- `npm run lint`: passing.
- `npm run build`: passing.
- `npm run test:integration`: 8 passing live PostgreSQL tests; not re-run this session (no schema changes), only the existing disposable container and unit/lint/build ladder were used.
- Browser workflow verified authentication, RLS-backed summary loading, one material price audit, and recent membership activity.
- Desktop screenshot at 1440x1000 was compared directly with the generated concept using `view_image`.
- Mobile QA recheck closed: restarted the API against the disposable database (recreated `app/.env` and reseeded), reran the 390x844 screenshot via an ad hoc Playwright driver, and measured `document.documentElement.scrollWidth` directly rather than relying on visual inspection alone.
- The original `.data-panel`/`.activity-panel`/`.table-scroll` wide-table fix held correctly.
- Found a second overflow source the prior session missed: `.sidebar`/`.sidebar nav` lacked `min-width:0` inside the `@media(max-width:760px)` block, so CSS Grid's auto-minimum sizing let the nowrap tab row push the single `1fr` track ~18px past the viewport even though the nav's own `overflow-x:auto` worked correctly in isolation. Fixed in `api/views/adminShell.view.ts`; `scrollWidth` now equals `clientWidth` (390px) exactly.
- Full verification ladder re-run after the fix: tests/lint/build all pass.
- Provisioning hardening: added rate limiting and an optional IP allowlist to `POST /api/v1/platform/organizations`. Live-verified against the running API — 5 wrong-secret requests return 401, the 6th returns 429, proving the limiter triggers before the secret check runs. `npm test` (61/61, 18 suites), lint, and build all pass.
- Supplier queue persistence: added the table/RLS/service/worker/HTTP surface described above. Live-verified the full HTTP flow against the running API (enqueue → list → approve → material price + audit updated correctly; re-approve correctly 409s; enqueue → reject → price left untouched) and extended `tests/rls.integration.ts` with a live test proving a viewer-role background job is blocked from enqueueing, an estimator-role job can enqueue but not approve, and admin approval is the only path that changes a real price under PostgreSQL RLS. `npm test` (68/68, 20 suites), `npm run test:integration` (9/9), lint, and build all pass. Found and documented a pre-existing gap: there is no supplier CRUD API at all (suppliers can currently only be created directly against the database).
- Closed that gap: added the supplier CRUD API described above. Caught and fixed a real bug while building it — `supplier_price_updates`' foreign keys to `suppliers`/`materials` had been set to `on delete cascade` in the same session that created them, which would have silently destroyed price-update history (including already-approved/rejected rows) the first time someone deleted a supplier or material. Changed to `on delete restrict`. Live-verified the full CRUD lifecycle and the restrict protection (enqueue a price update against a supplier, confirm delete then fails) against the running API, and added a live RLS test covering both the write-permission boundary and the restrict protection. `npm test` (73/73, 21 suites), `npm run test:integration` (10/10), lint, and build all pass.
- Added the scheduler/cron consumer described above. Live-verified three things against the real disposable database: the CLI script (`npm run jobs:supplier-price-sync`) runs a valid target successfully and a deliberately-bogus one fails independently, exiting 1 overall; the in-process scheduler is silent when unconfigured; and — critically — using a real `* * * * *` cron expression, the in-process timer actually fired on its own after a real minute boundary and ran both configured jobs, proving it's not just registered but genuinely executing on schedule. `npm test` (82/82, 22 suites), `npm run test:integration` (10/10), lint, and build all pass.
- Added the Prisma error mapper described above. Live-verified against the running API: creating a division with a duplicate `(orgId, code)` now returns a clean 409 (it previously 500'd); deleting a supplier with price-update history now returns a clean 409 with the violated constraint name, instead of the 500 left over from adding the RESTRICT foreign key a few turns ago. `npm test` (89/89, 23 suites), `npm run test:integration` (10/10), lint, and build all pass.
- Started and substantially completed production migration rollout automation. Asked the user to confirm direction first (Prisma Migrate vs. a custom mechanism; role provisioning in/out of scope) since this touched the project's foundational "hand-written SQL files are authoritative" model — both recommended options were chosen. Baselined `db/migrations/0001`/`0002` verbatim into tracked `prisma/migrations/` history; proved equivalence two ways (`pg_dump --schema-only` diff against the old manual-psql path came back identical apart from `pg_dump`'s random per-run restrict tokens, and the full live RLS integration suite passed 10/10 against a database built purely through `prisma migrate deploy`). Added idempotent role provisioning and a composite deploy script, then collapsed three independent copies of "create container, apply SQL, create role, grant" (the test harness, the run skill's `driver.mjs`, and the old manual docs) onto that one path. Deleted the now-redundant `db/migrations/` directory. Hit two real bugs along the way: psql doesn't substitute `:'var'` inside `DO $$...$$` blocks (worked around with `\gexec`), and psql 18+ rejects Prisma's `?schema=public` query param outright (stripped it before invoking psql). Also discovered the sandbox had no host `psql` client at all — installed via `brew install libpq` (keg-only) and documented as a new Prerequisite. `npm test` (89/89, 23 suites, unchanged), `npm run test:integration` (10/10, now exercising the real rollout path), lint, and build all pass.
- Wired that script into a GitHub Actions workflow, closing the immediate next-step item from the previous turn. Since this repo has no git remote, installed `actionlint` and `act` (`brew install actionlint act`) to verify as thoroughly as possible without one: `actionlint` static validation came back clean, and `act` actually ran the workflow's job inside a real GitHub Actions runner image (`catthehacker/ubuntu:act-latest`) against the disposable test database via `host.docker.internal` — both migrations applied, the role was provisioned, and a second run proved idempotency at the workflow level too ("No pending migrations to apply" + `ALTER ROLE`). Documented the exact `act` command in the workflow file's own header comment. `npm test` (89/89, 23 suites), `npm run test:integration` (10/10), lint, and build all still pass (no application code touched, only the new workflow file and docs).
- Asked the user to confirm direction (merge `/admin/member-history` into the shared shell, leave it, or do something else) since it was an open decision on the rolling TODO, not an assigned task — chose to merge. Extended `adminShellCss` with the CSS the page needed that didn't exist yet (chips, pagination, snapshot details, a 4-column access-form variant with matching responsive overrides), then rewrote the page's markup to use the shared `.admin-layout`/`.access-panel`/`.data-panel` chrome — every existing filter/pagination/snapshot helper function was left untouched, since this was a visual change only. One test assertion checked the old page's literal heading text and was updated to match the new copy; no other test or logic change. Live-verified visually (not just via tests): seeded data, created a real membership via the API to get an actual audit row so the populated-table state was checked (not just empty), and screenshotted at 1440×1000 and 390×844 — `scrollWidth === clientWidth` at both, table/chips/pagination/nav all render correctly. `npm test` (89/89, 23 suites), `npm run test:integration` (10/10), lint, and build all pass. This closes out the last item on the rolling TODO that was actually self-contained and actionable from inside this codebase.
- Added common assembly templates for quick adding to estimates (a direct user request, not from the rolling TODO). Found that "quick add an assembly to an estimate" was already fully solved by the existing `assemblyId` support on `addLineItem` — the actual gap was having a way to mark/discover assemblies as reusable templates. Added `isTemplate` on `Assembly` via a new hand-written migration (after `prisma migrate diff` showed the auto-diff wanted to touch primary keys/column types/constraint names across nearly the whole schema for purely cosmetic reasons — confirmed this was noise, not anything to actually apply), `listTemplates`/`GET /api/v1/assemblies/templates`, and one seeded example. This is also the first real schema change since the migration-rollout-automation work, proving that pipeline handles incremental changes correctly. Live-verified the full flow against the running API: queried templates, confirmed the seeded template's rolled-up unit cost (47.55 from two cost items), created an estimate, and quick-added the template as one line item in a single call. Added a live RLS test (viewer blocked from creating a template, cross-org visibility denied). `npm test` (93/93, 23 suites), `npm run test:integration` (11/11), lint, and build all pass.

## Admin UI Fidelity Ledger
- Layout: concept left rail, top bar, open table workspace, and right activity rail are implemented.
- Typography: condensed industrial display hierarchy and sturdy UI typography are implemented without external font dependencies.
- Palette: warm paper canvas, charcoal navigation, steel-blue secondary accents, and restrained orange actions match the concept.
- Data density: implementation is table-first and avoids generic card-grid/bento treatment.
- Interaction: token/org authentication, stale threshold, material/source/date filtering, membership-history navigation, and responsive navigation are functional.
- Intentional deviation: the concept assumed an established browser session; the implementation includes an explicit internal bearer-token access panel because this backend has no cookie/session frontend yet.
- Intentional deviation: summary metrics reflect currently available backend facts rather than invented totals or fake charts.

## Best Next Session Start
The rolling TODO's self-contained items are done. What's left is blocked on something external or genuinely deployment-specific — ask the user what they want to tackle rather than assuming:
1. Configure infrastructure-level network controls (security groups/ALB rules) around platform provisioning per deployment — the one piece of "production rollout" work that's genuinely deployment-specific. Migration/role rollout is now automated and wired into CI/CD (`scripts/deploy-migrations.sh` + `.github/workflows/deploy-migrations.yml`); rate limiting/IP allowlist for provisioning were already done.
2. Once this repo has a real GitHub remote: configure the `production` Environment's required reviewers, and trigger `.github/workflows/deploy-migrations.yml` for real — it's only ever been run locally via `act`, never against actual GitHub infrastructure.
3. Add a real `SupplierFeedFetcher` implementation once an actual supplier feed exists — the CRUD API, queue/review/audit/worker plumbing, scheduler, and clean error responses are all ready to receive real quotes now; only the live feed transport is missing.
4. If a new Prisma error code shows up in practice that should be mapped to a 4xx, add it to `mapPrismaKnownRequestError` in `api/middleware/errorHandler.ts` — deliberately only P2002/P2003/P2025 are handled today, since those are the only codes this app has actually hit.
5. Consider seeding a few more common assembly templates (deck, bathroom remodel, kitchen refresh) — only one exists today, just enough to prove the `isTemplate` feature works end-to-end.

## Watch Outs
- Never run tenant Prisma queries outside an HTTP, admin UI, or background database session once RLS is active.
- Never use PostgreSQL superuser credentials for application traffic or RLS tests.
- Deploy request-session code before or atomically with forced-RLS migrations.
- Keep `PLATFORM_PROVISIONING_SECRET` separate from JWT secrets, at least 32 characters, rotated, and restricted at the network edge.
- `PLATFORM_PROVISIONING_ALLOWED_IPS` relies on accurate `req.ip` — it only does anything useful if the app trusts a correctly configured proxy in front of it (Express `trust proxy`). Don't treat it as sufficient on its own; it's a backstop behind real network controls.
- Keep audit tables append-only; no update/delete policies exist by design.
- `supplier_price_updates` review (approve/reject) is restricted to admin/owner under RLS even though estimator+ can enqueue — this is deliberately tighter than ordinary tenant writes, since approval is what actually moves a live price. Don't loosen the review policy to match the enqueue policy without re-checking that intent.
- `supplier_price_updates.supplier_id`/`material_id` are `ON DELETE RESTRICT`, not `CASCADE` — deleting a supplier or material with any price-update history (including resolved rows) is supposed to fail, the same way `material_price_audits` protects materials. If you ever see this as `CASCADE` again, it's a regression, not a feature.
- The supplier price sync scheduler deliberately does not auto-discover organizations/suppliers — `SUPPLIER_PRICE_SYNC_JOBS` lists targets explicitly. Don't "simplify" this into a cross-tenant `organization.findMany()` scan using the app's normal restricted DB connection — under forced RLS with no `app.org_id` session set, that silently returns zero rows rather than erroring, which is exactly the kind of footgun this app's existing Watch Outs already warn about for tenant Prisma queries outside a request/background session.
- `prisma/migrations/` is the only authoritative migration history now — `db/migrations/` no longer exists. Never hand-edit an already-applied migration's `migration.sql`; add a new migration instead, the same as any other Prisma Migrate project.
- `DATABASE_ADMIN_URL` is for `scripts/deploy-migrations.sh`/`scripts/provision-app-role.sh` only — never the running API's `DATABASE_URL`. The restricted role can't run DDL or grant itself privileges, by design; that's exactly why these need a separate elevated connection.
- If you add a migration that introduces a new table needing the restricted role's access, you don't need to hand-edit grants — `scripts/sql/provision-app-role.sql` already sets `alter default privileges` for future tables/sequences/functions, and re-running `scripts/deploy-migrations.sh` (which always re-runs the grant statements too) catches anything that predates that default-privileges setting.
- `psql` must be on `PATH` for `scripts/deploy-migrations.sh`/`scripts/provision-app-role.sh` to run (host client, not `docker exec`-based) — on macOS via Homebrew it's keg-only (`brew install libpq`, not symlinked into `/opt/homebrew/bin` automatically).
- `.github/workflows/deploy-migrations.yml` has only ever been run via `act` against this repo's local disposable Postgres container, never against real GitHub infrastructure (no git remote exists yet) and never against a real production database. Treat it as unverified-in-the-target-environment until someone actually triggers it for real with the `production` Environment's reviewer gate configured — don't assume the local `act` pass means it's safe to point at a real database unattended.
- `/admin`, `/admin/pricing-history`, and `/admin/member-history` all now share one CSS file (`adminShellCss` in `api/views/adminShell.view.ts`). Changing a shared class (`.chip`, `.pagination`, `.access-panel form`, `.filters`, etc.) affects all three pages — check all of them, not just the one you're editing, before assuming a CSS tweak is page-local.
- Never run `prisma migrate dev` (or trust `prisma migrate diff` output) blindly against this schema without reading the diff first — because the two original migrations were hand-written SQL rather than Prisma-generated, Prisma's own diff engine sees the whole schema as drifted (wants to rewrite primary keys, change `uuid` columns to `TEXT`, rename every constraint/index) even when nothing real changed. This is cosmetic, not a bug to fix — write new migrations by hand (a fresh folder under `prisma/migrations/` + `migration.sql`), the same way the first two were written, and the same way `20260624083000_add_assembly_is_template` was added.
- Template assemblies (`isTemplate`) are org-scoped, not a shared cross-tenant catalog — a `NULL` `org_id` row would be invisible to every tenant under forced RLS regardless of intent. Don't try to add a "global" template library by setting `org_id` to `NULL`; it would silently show up nowhere.
- Keep long-running work outside HTTP transactions and comfortably below `RLS_TRANSACTION_TIMEOUT_MS`.
